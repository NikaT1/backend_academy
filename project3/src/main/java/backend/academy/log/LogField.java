package backend.academy.log;

public enum LogField {
    REMOTE_ADDR,
    REMOTE_USER,
    TIME_LOCAL,
    HTTP_METHOD,
    RESOURCE,
    HTTP_VERSION,
    STATUS,
    BODY_BYTES_SENT,
    HTTP_REFERER,
    HTTP_USER_AGENT
}
